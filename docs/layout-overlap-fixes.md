# Исправление наложений текста и layout (kanban t_d5226086)

Основание: аудит виджета (`ABG-widget-audit.md`, kanban `t_eedefa0a`), пункт **P0-3
«Наложения и обрезка текста»** — подпункты (a) нижняя полоса, (b) левая колонка,
(c) 3D-колауты. Изменения — только вёрстка/позиционирование, логика и 3D-контент не тронуты.

## Что сделано

| Файл | Изменение | Зачем |
|---|---|---|
| `src/App.tsx` | пилюля «ABG vs Газобетон»: `hidden sm:flex` → `hidden xl:flex` | на sm..lg она стояла в одной полосе с мобильным баром и контролами сцены (замеры 1972/1516 px²). Само действие осталось в правом рейле («ABG VS ГАЗОБЕТОН (ТАБЛИЦА)») и в мобильном drawer |
| `src/components/Panel3D/PanelScene.tsx` | HUD материалов + подсказка жестов: `bottom-4 left-4` → `top-28 sm:top-32 left-3`; контролы сцены: `bottom-4` → `bottom-[4.5rem] lg:bottom-4`; контейнеру контролов присвоен `id="stage-controls-dock"`; при открытом «Сечении» HUD скрывается ниже 1280 px | нижняя полоса освобождена: слева CTA, справа контролы, по центру мобильный бар — по одной сущности на зону, без перекрытий |
| `src/components/UI/LayerDetailCard.tsx` | `bottom-6` → `bottom-[7.5rem] lg:bottom-24` | карточка выбранного слоя больше не садится на мобильный бар и на контролы сцены |
| `src/components/UI/LeftAnatomyRail.tsx` | `min-w-0` у ряда и заголовка, `shrink-0 whitespace-nowrap` у бейджа толщины, `break-words` у h3 | двухстрочный заголовок слоя больше не вылезает из карточки, «120–150 мм*» не ломается на две строки |
| `src/components/Panel3D/SwissCallout.tsx` | убран `distanceFactor={4.4}` (плашка постоянного размера); `max-w` вместо `sm:max-w-none`; убран `truncate` у спецификации; общий резолвер раскладки: флип стороны у края сцены, кламп в границы центральной сцены, разведение плашек по вертикали, обход панелей, скрытие на узких экранах при открытом «Сечении» | колауты больше не уезжают за вьюпорт (было до x = −107 px и 12 узлов вне экрана на 390), не накрывают боковые рейлы (было 4 наложения до 1175 px² на 1366) и не режут текст (было 4 обрезки 68–92 px) |
| `src/components/Panel3D/PanelModel.tsx` | у 3D-тегов (габаритный «Контур 390 мм», температурные «Снаружи / Точка 0 °C / Интерьер») `zIndexRange={[15, 0]}`; габаритный тег скрыт на `<sm` | панели интерфейса (панель среза, контролы сцены) всегда рисуются выше аннотаций на канвасе; на телефоне габаритный тег не вылезал за экран |

Раскладка колаутов считается **одним общим проходом** (модульный singleton в `SwissCallout.tsx`)
на собственном `requestAnimationFrame` (~8 раз/с), а не в `useFrame`: раскладка не должна
зависеть от того, крутится ли цикл рендера WebGL (в фоновой вкладке он останавливается), и
один проход исключает расхождение раскладок между отдельными плашками.

## Как проверялось

Сканер из приложения A аудита (`overlaps > 250 px²`, `clipped`, `outside viewport`),
локальный dev-сервер, headless-браузер с реальным WebGL, вьюпорты **360, 390, 768, 1024,
1366, 1600** × режимы **РАЗОБРАН / СБОРКА / АРМАТУРА / ТЕПЛО**, плюс состояния «выбран слой»,
«открыто Сечение» и зум колесом.

- до правок: 1600×900 — 2 наложения (1972 и 1516 px²); 390×844 — 3 наложения и 12 узлов вне экрана;
- после правок: **в 4 основных режимах на всех целевых вьюпортах — 0 наложений, 0 обрезок,
  0 вылетов за вьюпорт** (полный лог: `scan-final*.json` в рабочей папке задачи, kanban `t_d5226086`).

## Сознательные исключения (не дефекты)

1. **Режим «Сечение» на узком экране:** панель инструмента (z-30, непрозрачная) перекрывает
   канвасные аннотации — это фокусный режим, плашки слоёв на таких ширинах скрываются
   (`< 1280 px` центральной сцены), хотспоты остаются. Температурные 3D-теги остаются под
   панелью (occlusion, не наложение текста).
2. **Модальные окна** (калькулятор, консультация, шит сравнения, мобильный drawer) — оверлеи
   поверх сцены по назначению.
3. **360 px** (уже 390-го): в режимах «РАЗОБРАН/АРМАТУРА» возможна одна пара
   плашка≈контрол из-за нехватки места; на 390+ чисто.

## Доработка после слияния с main (второй проход)

После merge с `origin/main` (ветка `fix/overlap-layout` → `0b09291`) сканер нашёл ещё
четыре пересечения в состояниях, которые первый проход не покрывал — «выбран слой»
и «Сечение открыто». Все четыре закрыты по одной политике **«один голос за раз»**:
в каждой зоне сцены остаётся ровно одна подпись, остальные уступают место или прячутся.

| Файл | Изменение | Зачем |
|---|---|---|
| `src/components/Panel3D/InvisibleHotspot.tsx` | новый проп `allowPopover`, поповер не рендерится при `compactStage` | поповер шириной 288–320 px привязан к кольцу хотспота: на 390/768 он вылезал за вьюпорт (5 узлов) и накрывал текстом плашки слоёв (6 пересечений до 1037 px²). Те же данные на узком экране показывает `LayerDetailCard` |
| `src/components/Panel3D/PanelModel.tsx` | `compactStage = viewportSize.width < 1024`; температурные теги скрываются при открытой панели «Сечение» и сцене < 1280 px (та же политика, что у `SwissCallout`); габаритная плашка «Контур 390 мм» переехала вниз по центру (`[0, -1.42, 0]`), включается с `lg` и гаснет при выбранном слое | на 1024 `-20 °C`/`0 °C` наезжали на подсказку жестов; на 1600 в СБОРКЕ «(70 + 200 + 120)*» упиралась прямо в «Сечение / 3D Обзор» (393/365/266 и 458/286 px²) |
| `src/components/Panel3D/PanelScene.tsx` | ширина сцены меряется `ResizeObserver`; в режиме «ТЕПЛО» HUD материалов и подсказка жестов скрываются, когда сцена сжата (< 640 px — это 1024–1279 px окна, где оба рейла съедают ~620 px) | теги температур — главная аннотация теплового режима, HUD уступает им зону |
| `src/components/Panel3D/SwissCallout.tsx` | `LayerDetailCard` добавлен в список препятствий (`layer-detail-dock`); если ни одна позиция не влезает без наложения > 60 px² — плашка скрывается | на 390 карточка выбранного слоя занимает почти всю ширину сцены, и плашки ложились текстом на её текст |
| `src/components/UI/LayerDetailCard.tsx` | `id="layer-detail-dock"` | тот же резолвер раскладки, что уже обходит панель среза и док контролов |

Проверено тем же сканером (>250 px²) в headless-браузере с реальным WebGL:
**360, 390, 768, 1024, 1366, 1600 × РАЗОБРАН / СБОРКА / АРМАТУРА / ТЕПЛО + три
состояния (выбран слой, открыто Сечение, реальный путь через drawer «Анатомия») —
30 прогонов, 0 наложений, 0 обрезок, 0 вылетов за вьюпорт** (лог `scan-final2.jsonl`).
`bunx tsc --noEmit` чист, `bun test` 35/35, `bun run build` зелёный.

## Что осталось за кадром (для следующих задач)

- P0-1 (локальный HDRI + ErrorBoundary) и P0-2 (реальный канал заявок) — отдельные задачи,
  не трогались.
- Единый набор текстов слоёв/температур (P2-8) — не трогался.
