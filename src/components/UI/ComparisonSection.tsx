import React from 'react';

/**
 * Safe extension point for host pages that still import this component.
 * A public comparison must be based on a verified project brief rather than
 * generic construction-market claims.
 */
export const ComparisonSection: React.FC = () => null;
